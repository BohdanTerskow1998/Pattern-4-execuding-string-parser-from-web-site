В шаблоне используются языки HTML, CSS, PHP.
Основная задача: осуществить парсер строк с выбранного сайта.

The template uses HTML, CSS, PHP.
Main task: execute a string parser with the selected site.